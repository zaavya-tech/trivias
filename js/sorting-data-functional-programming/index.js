/**
 * Given: A list of users array of objects.
 * @Requirement: Sort users by age and filter only female profiles and create a new list of users 
  (not mutating the existing users list). You have to implement this using functional programming, also create a new array
  of users without mutating the provided lists. 
  
*/

const users = require('./users.json');

function main() {
  /* Your code goes here! */
}

main(users);